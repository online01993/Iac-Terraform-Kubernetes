network:
  version: 1
  config:
  - type: physical
    name: eth0
    subnets:
      - type: static
        address: ${node_address}
        netmask: ${node_mask}
        gateway: ${node_gateway}
  - type: nameserver
      #interface: interface0  # Ties nameserver to interface0 only
      address:
        - ${node_dns_address}
      #search:
      #  - exemplary     